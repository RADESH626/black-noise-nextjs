import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import SessionProviderWrapper from './SessionProviderWrapper' // Ajusta la ruta si tu estructura es diferente
import { getServerSession } from 'next-auth/next'
import { PopUpProvider } from '../context/PopUpContext'
import { ModalProvider } from '../context/ModalContext'
// Asegúrate de que la ruta a authOptions es correcta.
// Si tu API route está en src/app/api/auth/[...nextauth]/route.js
// y layout.jsx está en src/app/layout.jsx, la ruta es './api/auth/[...nextauth]/route'
// o directamente desde donde lo exportaste.
import { authOptions } from './api/auth/[...nextauth]/route' // Ajusta esta ruta si es necesario

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"], 
});

export const metadata = {
  title: 'Black Noise App', // Puedes cambiar esto
  description: 'Generated by create next app', // Puedes cambiar esto
}

export default async function RootLayout({ children }) {
  // Obtenemos la sesión en el servidor para pasarla al SessionProviderWrapper
  const session = await getServerSession(authOptions)

  return (
    <html lang="es">
      <body>
        <PopUpProvider>
          <ModalProvider>
            <SessionProviderWrapper session={session}>
              {children}
            </SessionProviderWrapper>
          </ModalProvider>
        </PopUpProvider>
      </body>
    </html>
  )
}
